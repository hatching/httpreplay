language: python

python:
    - 3.6

install:
    - pip3 install -e .
    - pip3 install mock pytest==4.4.1 pytest-cov coveralls

script:
    - py.test --cov=httpreplay
    - pip3 install '.[mitmproxy]'
    - pip3 install '.[dev]'
    - py.test --cov=httpreplay --cov-append

after_success:
    - coveralls
